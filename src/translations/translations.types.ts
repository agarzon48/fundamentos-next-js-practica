export type Translations = {
  home: {
    hero: {
      title: string;
      subtitle: string;
      text: string;
      button_buy: string;
      button_learn_more: string;
    };
  };
};

export type AvailableLocales = "es" | "en";
